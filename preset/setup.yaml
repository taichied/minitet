# Remove the next line once you have a show/buffer control
show/buffer: paint

window:
  title: minitet
  width: 640
  height: 200
  fullscreen: false

# Here we define white, which comes in handy.
pixels/white:
  format: hex2
  data: fff
